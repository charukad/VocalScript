# UI Primitives

- Use `Button` for primary, secondary, and ghost actions.
- Use `IconButton` for compact tool actions.
- Prefer `lucide-react` icons instead of adding one-off SVG markup for new editor controls.
- Keep new controls on the shared token system from `frontend/src/index.css`.
